# Base URL of the FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# When true, the app uses static mock data from src/mocks instead of calling the backend.
# Use this for frontend development before every endpoint exists.
VITE_USE_MOCK_DATA=false
