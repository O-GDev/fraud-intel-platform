# Base URL of the FastAPI backend
VITE_API_BASE_URL=https://fraud-detection-backend-dwgv.onrender.com

# The frontend defaults to static mock data so it works without a backend.
# Set this to false when the FastAPI service is available.
VITE_USE_MOCK_DATA=false
